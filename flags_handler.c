#include "main.h"
#include <stdarg.h>

/**
 * flags_handler - handles all flags
 * @str: the string to format with flags
 * @flag_struct: structure containing flags
 *
 * Return: number of characters printed
 */
int flags_handler(char *str, type_flags flag_struct)
{
	
